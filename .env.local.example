# Copy to .env.local and fill in.

# Required for the default Whisper transcriber.
OPENAI_API_KEY=sk-...

# Optional: which speech engine to use. Currently: "whisper" (default).
# DUGSI_TRANSCRIBER=whisper
