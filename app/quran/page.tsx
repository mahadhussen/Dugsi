import QuranReader from "@/components/QuranReader";

export const metadata = {
  title: "Dugsi — Read the Quran",
  description: "Listen to the Sheikh or recite yourself, on the pages of the printed mushaf, with every word checked.",
};

export default function QuranPage() {
  return <QuranReader />;
}
